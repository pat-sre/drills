# Training Setup
#
# Build a complete training pipeline from scratch.
# Create model, loss function, optimizer, train, and return the model.
#
import torch
import torch.nn as nn


def solve(X: torch.Tensor, y: torch.Tensor, epochs: int = 100) -> nn.Module:
    pass
