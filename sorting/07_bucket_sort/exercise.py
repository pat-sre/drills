# Implement bucket sort.
#
# Given a list of floats in [0, 1), return a new sorted list.
# Do not modify the input list.
#
# Algorithm:
#   1. Create n empty buckets (n = len(nums)).
#   2. Scatter: place each element into bucket floor(x * n).
#   3. Sort each bucket (insertion sort or built-in).
#   4. Gather: concatenate all buckets in order.
#
def solve(nums):
    pass
